# API Configuration
VITE_API_URL=http://localhost:8000/api

# Mode fallback - utilise des données mockées si l'API n'est pas disponible
# Définissez à "true" pour utiliser les données de test, "false" pour utiliser l'API réelle
VITE_USE_FALLBACK=true

# Laravel Backend Configuration
# Assurez-vous que votre backend Laravel fonctionne sur cette URL
# Exemple pour un serveur Laravel local : http://localhost:8000/api
# Exemple pour un serveur de production : https://votre-domaine.com/api
